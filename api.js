// Script only runs if connected / session open
/*
r = ptp.getDeviceInfo();
r = ptp.customSend(0x1005, [1, 2, 3, 4, 5], [0x0, 0x0, 0x0, 0x12]);
bytes = ptp.customGet(0x1005, [1, 2, 3, 4, 5]);
ptp.disconnect();
ptp.capturePhoto();
*/

setInterval(function() {
    ptp.
}, 100);

